# hold 5
# luccas, martin, matias
import math

def KegleStubV(V, R, r, h):
    resultat = 0
    #V = pi/3 * h * (R^2 + r^2 + R * r)

    if V == 0:
        resultat = math.pi / 3 * h * (R ^ 2 + r ^ 2 + R * r)
    elif R == 0:


        resultat =

    return resultat
